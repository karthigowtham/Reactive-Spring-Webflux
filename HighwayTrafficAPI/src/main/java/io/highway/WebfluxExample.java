package io.highway;

public class WebfluxExample {

	/*
	 * public static void main(String[] args) throws InterruptedException {
	 * 
	 * Mono<Integer> mono = Mono.just(2); mono.map(i -> i *
	 * 2).subscribe(System.out::println, null, () ->
	 * System.out.println("MONO completed"));
	 * 
	 * Flux<Integer> flux = Flux.just(1, 2, 3, 4, 5, 6, 7, 8, 9); flux.map(i -> i *
	 * 2).subscribe(System.out::println, null, () ->
	 * System.out.println("FLUX completed"));
	 * 
	 * Flux.interval(Duration.ofSeconds(2)).map(i -> i *
	 * 2).subscribe(System.out::println, null, () ->
	 * System.out.println("FLUX completed"));
	 * 
	 * Thread.sleep(10000);
	 * 
	 * }
	 */
}
